# Configuration file for GameOfLife random example

# Define grid size
n_rows = 10
n_cols = 20

# Random initial state?
random_init = false

# If false:
init_file = spaceship.input

# Logging
# How often to log
log_every = 1

# Number of milliseconds to sleep after printing log (valid only for printing)
log_sleep = 50

# Log to file?
log_to_file = false

# Run
run = 5000
